node_modules
dist
*.tsbuildinfo
/runs/
*.local
.DS_Store

# .env produksi di root (dipakai docker-compose.prod.yml). Templatnya
# .env.production.example yang ter-commit.
/.env

# Rahasia Docker — JANGAN commit API key. Pakai .env.roboflow.example sbagai templat.
docker/.env.roboflow
docker/.env

# Hash password broker MQTT. Templat + cara generate ada di passwd.example.
# `acl` sengaja TIDAK diabaikan: ia kebijakan, bukan kredensial, dan justru
# perlu ditinjau lewat review.
docker/mosquitto/passwd